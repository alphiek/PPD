import React from "react";
import Nav from "./Nav";
import styled from 'styled-components'

const Header = () => {
  return (
    <header>
        <Nav />
    </header>
  );
};

export default Header;

